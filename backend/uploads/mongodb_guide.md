# Knovera MongoDB Vector Search
Knovera uses MongoDB Atlas as its high-performance enterprise vector database.
All document embeddings are stored directly in MongoDB collections.
Queries are matched using cosine similarity and nearest-neighbor vector search.
